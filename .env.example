# Copy to .env and adjust. Consumed by scripts/deploy.sh.

# Target Soroban network: testnet | futurenet | mainnet | local
NETWORK=testnet

# Stellar CLI identity alias used to sign deploy/init transactions.
# Created and funded automatically on testnet if it does not exist.
IDENTITY=rwa-admin
